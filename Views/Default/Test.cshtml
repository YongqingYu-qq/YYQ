@using WebApplication_Core_mvc.Models
@model WebApplication_Core_mvc.Controllers.ClassModel
@{
    ViewData["Title"] = "Test";
    Layout = "~/Views/Shared/_Layout.cshtml";
    int rowIndex = 0;
}

<h1>Test</h1>

@*<form id="register">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>

    <div class="form-row">
        <div class="col-md-3">
            <div class="form-group">
                <label for="exampleInput1" class="bmd-label-floating">Category *</label>
                <select asp-for="TaskTicket.CategoryId" class="custom-select" id="category" asp-items="@ViewBag.CategoryId">
                    <option selected="selected" value="">Select Category</option>
                </select>
                <span asp-validation-for="TaskTicket.CategoryId" class="text-danger"></span>
            </div>
        </div>

        <div class="col-md-3">
            <div class="form-group">
                <label for="exampleInput1" class="bmd-label-floating">Site *</label>
                <select asp-for="TaskTicket.SiteID" class="custom-select" id="site" asp-items="@ViewBag.SiteID">
                    <option selected="selected" value="">Select Site</option>
                </select>
                <span asp-validation-for="TaskTicket.SiteID" class="text-danger"></span>
            </div>
        </div>

    </div>



    <div class="row">
        <div class="col-md-8">

            <table id="datatables" class="table table-striped table-no-bordered table-hover" cellspacing="0" width="100%" style="width:100%">
                <thead>
                    <tr>
                        <th>S/N</th>
                        <th>Select</th>
                        <th>
                            Description of Item
                        </th>
                        <th>Unit</th>
                        <th>Quantity</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.ProjectList)
                    {
                        <tr>
                            <td>@(rowIndex++)</td>
                            <td>  <input type="checkbox" class="checkBox" name="check" value="@item.TaskProjectId" /></td>
                            <td>
                                @Html.DisplayFor(modelItem => item.ItemDescription)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Unit)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Quantity)
                            </td>

                        </tr>
                    }
                </tbody>
            </table>

        </div>
    </div>

    <div class="form-row">

        <div class="fileinput fileinput-new text-center" data-provides="fileinput">
            <div class="fileinput-new thumbnail">
                <img src="~/assets/img/image_placeholder.jpg" alt="..." id="blah">
            </div>
            <div class="fileinput-preview fileinput-exists thumbnail"></div>
            <div>
                <span class="btn btn-rose btn-round btn-file btn-sm">
                    <span class="fileinput-new">Upload Image</span>
                    <span class="fileinput-exists">Change</span>
                    <input type="file" class="custom-file-input"
                           onchange="document.getElementById('blah').src = window.URL.createObjectURL(this.files[0]), checkPhoto(this)"
                           asp-for="TaskTicket.ImageUrl" />
                </span>
                <span class="text-danger" id="photoLabel"></span>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-md-4">
            <div class="form-group">
                <label asp-for="TaskTicket.SeverityId" class="control-label"></label>
                <select asp-for="TaskTicket.SeverityId" class="custom-select" id="severity" asp-items="@ViewBag.SeverityId">
                    <option selected="selected" value="">Select Task Priority</option>
                </select>
                <span asp-validation-for="TaskTicket.SeverityId" class="text-danger"></span>
            </div>
        </div>
    </div>

    <div class="form-group">
        <input type="submit" value="Create" id="register" class="btn btn-primary" />
    </div>
</form>*@